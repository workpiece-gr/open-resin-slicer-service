# Resin profiles

A resin profile consists of `<id>.json` plus `<id>.ini`. Exposure, initial exposure, transition behavior and related settings are machine/resin specific. Do not copy exposure values from a different printer and mark them production-ready.
