# Quality profiles

Quality profiles own layer-height and process-level SLA settings. The initial Workpiece UI can continue to expose draft/balanced/fine labels, but each label must resolve to an explicitly validated profile for the selected machine/resin combination.
